# Start with Fedora 40 (native ARM64 support for FlightGear)
FROM quay.io/fedora/fedora:latest

# Install FlightGear and Subversion
RUN dnf install -y FlightGear subversion dbus-x11 mesa-dri-drivers glx-utils &&\
    dnf -y clean all

# Download the F-35B
RUN svn co https://svn.code.sf.net/p/flightgear/fgaddon/trunk/Aircraft/F-35B /usr/share/games/flightgear/Aircraft/F-35B

# When this container starts, immediately launch the simulator
ENTRYPOINT ["dbus-launch", "fgfs", "--aircraft=F-35B", "--airport=KCOS", "--runway=35R", "--timeofday=noon"]